{
  "name": "get_test_item",
  "version": "1.0.0",
  "description": "Lambda to grab item from dynamodb table with matching test key",
  "dependencies": {
    "aws-sdk": "^2.910.0"
  },
  "jest": {
    "moduleNameMapper": {
      "/opt/(.*)": "<rootDir>/common/$1"
    }
  },
  "devDependencies": {  
    "aws-sdk-mock": "^4.0.0",
    "jest": "^27.0.4"
  }
}
